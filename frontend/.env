# Backend API base URL (no trailing slash). In dev we also proxy /api
# to the backend via Vite, so this defaults are already correct.
VITE_API_URL=http://localhost:5000/api
# admin@maxspace.com 
#  admin123